import styled from 'styled-components'
export const Cabec = styled.header`
  background-color: ${(props) => props.theme.SecondaryColor};
  color: ${(props) => props.theme.MainColor};
  text-align: center;
  padding: 24px 0px;
`
